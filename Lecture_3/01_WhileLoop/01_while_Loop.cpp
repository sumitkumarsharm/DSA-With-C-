#include <iostream>
using namespace std;
int main()
{
    // while loop : - if we want to print a task agin and again than we use loop
    // printing 1 to 5
    int n = 1;
    while (n <= 5)
    {
        cout << n << endl;
        n = n + 1;
    }

    // sum from starting number to after 10
    // int n;
    // cout << "Enter a number: ";
    // cin >> n;
    // int sum = 0;
    // while (n <= 10)
    // {
    //     sum = sum + n;
    //     n++;
    // }
    // cout << "Sum is: " << sum << endl;

    return 0;
}