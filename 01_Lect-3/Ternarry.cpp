#include <iostream>
using namespace std;
int main()
{
    // ternary : writting a short if else statment into a single line is colled ternary operator but we not use is more!
    int n = 12;
    cout << (n % 2 == 0 ? "Even Number" : "Odd Number" ) << endl;
    return 0;
}